---
title: "OS. Stack, Heap 메모리 관리"
date: 2025-02-02

categories:
    - Computer Science
series:
    - Operating System
tags:
    - Operating System
    - Virtual Memory

draft: true
original: "notion-export/블로그 이관/ComputerScience/OS/stack, heap 메모리 관리 18eade8f376580c6bb01c607cbafaeb3.md"
---

Date: February 2, 2025
Multi-select: Memory

## 스택 메모리

LIFO 구조로, 함수 호출 시 생성되는 스택 프레임(로컬 변수, 파라미터, 반환 주소 등)을 저장한다. 함수가 호출 될 때 마다 새로운 스택 프레임이 쌓이고, 함수가 종료되면 스택 프레임 제거된다.

### 특징

- 빠른 메모리 할당/해제: 스택은 메모리가 자동으로 관리되어서 함수가 끝나면 알아서 메모리가 반환됨
- 고정 크기: 스택 메모리는 크기가 제한적이라서 너무 많은 데이터를 저장하면 스택 오버플로우 위험이 있음
- 지역 변수 저장: 함수 내부에서 선언된 변수는 스택에 저장되고, 함수 종료 시 자동으로 소멸됨
- Thread-safe: 각 스레드는 자신만의 스택을 가지기 때문에 스레드 간의 충돌이 없음

### 스택 오버플로우

프로그램이 스택 메모리를 초과할 때 발생하는 오류, 함수 호출 시마다 생성되는 스택 프레임이 쌓이면서 스택 공간을 사용하게 되고, 이 제한된 스택 공간을 초과해서 스택 프레임이 쌓일 경우 발생한다. 

너무 깊은 재귀 호출이나 대용량 지역 변수를 선언했을 때 스택 오버플로우 문제가 발생한다.

스택 오버플로우를 방지하는 방법은 다음과 같다:

- 재귀 호출 대신 반복문 사용하기
- 큰 데이터는 힙에 저장하기

### 스택 버퍼 오버플로우

스택에 할당된 메모리 버퍼(고정 크기)의 경계를 초과하여 데이터를 쓰는 상황, 메모리 손상, 예기치 않은 동작, 악의적인 코드 실행을 동반할 수 있음

- 스택 버퍼: 스택 메모리에 할당된 고정 크기의 메모리 공간 함수 내부에서 선언된 고정 크기 배열이나 데이터 블록이 스택 메모리에 저장됨

스택 버퍼 오버플로우를 방지하는 방법은 다음과 같다:

- 스택 카나리: 함수 호출 시 스택 프레임에 특수한 값(카나리)를 삽입해서, 버퍼 오버플로우가 발생했는지를 감지하는 기술, 스택 프레임과 지역 변수 사이에 카나리를 삽입하여 함수 종료시 카나리 값이 변경되었는지를 확인한다. (Swift에서 안씀)
- 언어 차원의 메모리 안정성
    - 배열 경계 경사
    - 옵셔널로 널 포인터 참조 방지
    - 타입 안정성

## 힙

동적 메모리 할당에 사용되는 영역, 데이터의 수명과 관리 방식이 다르기 때문에 앱 성능과 메모리 관리에 중요한 역할을 한다.

### 특징

- 동적 할당
    - 런타임동안 필요에 따라 메모리를 유연하게 할당하고 해제
- 수명 관리
    - ARC을 통해 수명 관리
- 비교적 느린 접근 속도
    - 포인터를 통해 접근하고 메모리 단편화 발생 가능성 잇음

### 메모리 누수

순환 참조와 같은 경우에 메모리 누수가 생길 수 있다.

### 메모리 단편화

동적으로 할당하고 해제되면서 메모리 단편화가 발생할 수 있다. 충분한 메모리가 남아있어도 연속된 큰 블록을 할당할 수 없을 수 있다.

- 맥에서는 페이지 기반으로 사용하기 때문에 내부 단편화가 발생한다.

### 힙 오버플로우

힙에 할당된 메모리 블록의 경계를 초과해서 데이터를 쓰는 현상, 다른 힙 블록이나 힙 메타데이터가 손상될 수 있음.

### 힙 버퍼 오버플로우

힙에 동적으로 할당된 버퍼의 경계를 초과해서 데이터를 쓰는 상황

- 스위프트에서는 메모리 안정성을 보장하기 때 일반적으로 힙 오버플로우가 발생하지 않음

## 레퍼런스

[About the Virtual Memory System](https://developer.apple.com/library/archive/documentation/Performance/Conceptual/ManagingMemory/Articles/AboutMemory.html)
