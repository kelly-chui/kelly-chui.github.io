---
title: "TCP/IP 4계층"
date: 2024-03-13

categories:
    - Computer Science
series:
    - Network
tags:
    - Network
    - TCP/IP

draft: true
original: "notion-export/블로그 이관/ComputerScience/Network/TCP IP 4계층 89e609d0242740db8352e13035ecadcf.md"
---

## 소개

TCP/IP 4계층은 OSI 7계층과 더불어 인터넷에서 데이터를 주고받기 위해 사용하는 대표적인 네트워크 참조 모델이다.

네트워크 통신 과정을 역할에 따라 계층으로 분리하여 관리하며, 각 계층은 독립적인 역할을 수행한다. 

덕분에 특정 계층의 구현이 변경되더라도 다른 계층에 미치는 영향을 최소화할 수 있다.

## 계층 구성

### 애플리케이션(Application)

- 대표 프로토콜: HTTP, HTTPS, DNS, FTP, SMTP

응용 프로그램이 사용되는 프로토콜 계층 이메일, 웹 서비스 등 서비스를 실질적으로 제공

### 전송(Transport)

- 대표 프로토콜: TCP, UDP

송신자와 수신자 간의 데이터 전송을 담당하는 계층

### 인터넷(Internet)

- 대표 프로토콜: IP, ICMP, ARP

IP, ARP, ICMP 등 장치로부터 받은 네트워크 패킷을 IP 주소로 지정된 목적지로 전송하기 위해 사용되는 계층

패킷을 수신해야 할 상대의 주소를 지정하여 데이터를 전달함. 상대방이 제대로 받았는지에 대해 보장하지 않는 비연결형적인 특징이 있음.

### 링크(Link)

- 대표 기술: Ethernet, Wireless

같은 네트워크(LAN)에서 데이터를 전달하고 실제 통신 매체를 통해 데이터를 전송하는 계층

## 캡슐화와 역캡슐화

애플리케이션 계층에서 전송 계층으로 리퀘스트 값들이 캡슐화 과정을 거쳐 전달 → 링크 계층을 통해 서버와 통신 → 해당 서버의 링크 계층으로부터 애플리케이션 계층까지 비캡슐화 과정을 거쳐 데이터 전송

### 캡슐화 과정

상위 계층의 헤더와 데이터를 하위 계층의 데이터 부분에 포함시키고 해당 계층의 헤더를 삽입하는 과정

1. 애플리케이션 계층 → 전송 계층: TCP(L4) 헤더 붙음, ‘세그먼트(데이터그램)화’
2. → 인터넷 계층: IP(L3) 헤더가 붙음, ‘패킷화’
3. → 링크 계층: 프레임 헤더와 프레임 트레일러가 붙음, ‘프레임화’

메시지 → 세그먼트(데이터그램) → 패킷 → 프레임

### 비캡슐화 과정

프레임 → 패킷 → 세그먼트(데이터그램) → 메시지

## PDU

네트워크의 계층에서 계층에서 전달될 때 한 덩어리의 단위를 PDU(Protocol Data Unit)라고 함

PDU는 헤더와 페이로드로 구성됨

- 애플리케이션 계층: 메시지
- 전송 계층: 세그먼트(TCP), 데이터그램(UDP)
- 인터넷 계층: 패킷
- 링크 계층: 프레임(데이터 링크 계층), 비트(물리 계층)
